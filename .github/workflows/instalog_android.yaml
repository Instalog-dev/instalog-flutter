name: instalog_android

on:
  pull_request:
    paths:
      - ".github/workflows/instalog_android.yaml"
      - "instalog_android/**"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/instalog_android.yaml"
      - "instalog_android/**"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: instalog_android
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - name: Install dependencies
        run: flutter pub get
      - name: Analyze
        run: flutter analyze
      - name: Run tests
        run: flutter test
      - name: Check code formatting
        run: flutter format --set-exit-if-changed .

